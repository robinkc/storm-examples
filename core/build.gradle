dependencies {
    compile group: 'org.apache.storm', name: 'storm-core', version: STORM_VERSION
}

tasks.withType(ScalaCompile) {
    scalaCompileOptions.with {
        additionalParameters = ["-Yresolve-term-conflict:package"]
    }
}